#include<stdio.h>
#include<conio.h>

int main()
{
	int a;
	long b;
	char c;
	double d;
	short e;
	float f;
	int long g;
	
	printf("the size of int is =%d.\n",sizeof(a));
		printf("the size of long is =%d\n",sizeof(b));
			printf("the size of char is =%d\n",sizeof(c));
				printf("the size of double is =%d\n",sizeof(d));
					printf("the size of short is =%d\n",sizeof(e));
						printf("the size of float is =%d\n",sizeof(f));
							printf("the size of int long is =%d\n",sizeof(g));
							getch();
}